# Minimal dependencies for Vercel Serverless
# Total size: < 100MB (estimated)

# Data Validation
pydantic[email]==2.10.0
pydantic-settings==2.6.0

# Database (Essential)
sqlalchemy==2.0.25
asyncpg==0.29.0
alembic==1.13.1

# Authentication & Security (Essential)
bcrypt==4.1.2
python-jose[cryptography]==3.3.0
passlib==1.7.4
argon2-cffi==23.1.0

# HTTP Client
httpx==0.26.0
python-multipart==0.0.6

# AI Services (Optional - comment out if not needed)
google-generativeai==0.4.0
instructor==1.7.0

# Document Processing (Optional - only if upload endpoint used)
PyPDF2==3.0.1
beautifulsoup4==4.12.3

# Utilities
python-dotenv==1.0.0
tenacity==9.0.0

# Remove these if still over 250MB:
# - google-generativeai (saves ~50MB)
# - instructor (saves ~30MB)
# - PyPDF2, beautifulsoup4 (saves ~10MB)
