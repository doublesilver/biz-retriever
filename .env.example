# ===========================================
# Biz-Retriever 환경 변수 설정
# ===========================================
# 이 파일을 복사하여 .env 파일로 저장 후 실제 값을 입력하세요.
# cp .env.example .env

# -------------------------------------------
# 기본 설정
# -------------------------------------------
PROJECT_NAME="Biz-Retriever Backend"
API_V1_STR="/api/v1"

# 보안 키 - 반드시 변경하세요! (openssl rand -hex 32 로 생성 권장)
SECRET_KEY="your-super-secret-key-change-this-in-production-min-32-chars"

# 토큰 만료 시간 (분) - 기본 8일
ACCESS_TOKEN_EXPIRE_MINUTES=11520

# -------------------------------------------
# 데이터베이스 (PostgreSQL)
# -------------------------------------------
# 로컬 개발 시
POSTGRES_SERVER=localhost
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_strong_password_here
POSTGRES_DB=bizmatch
POSTGRES_PORT=5432

# Docker 사용 시 (docker-compose에서 자동 설정)
# POSTGRES_SERVER=db

# -------------------------------------------
# Redis
# -------------------------------------------
REDIS_HOST=localhost
REDIS_PORT=6379
# Redis 비밀번호 (선택, 프로덕션에서는 설정 권장)
# REDIS_PASSWORD=your_redis_password

# Docker 사용 시
# REDIS_HOST=redis

# -------------------------------------------
# CORS 설정
# -------------------------------------------
# 허용할 도메인 (콤마로 구분)
CORS_ORIGINS=["http://localhost:8000","http://localhost:3000"]
# 프로덕션 도메인 (배포 시 설정)
# PRODUCTION_DOMAIN=https://your-production-domain.com

# -------------------------------------------
# OpenAI API (선택 - RAG 분석용)
# -------------------------------------------
# https://platform.openai.com/api-keys 에서 발급
OPENAI_API_KEY=sk-your-openai-api-key-here

# -------------------------------------------
# G2B API (나라장터 공공데이터)
# -------------------------------------------
# 발급 방법: https://www.data.go.kr/ 에서 회원가입 후 신청
# 활용 신청서에 "입찰 공고 수집" 목적 명시
G2B_API_KEY=your_g2b_api_key_here
G2B_API_ENDPOINT=https://apis.data.go.kr/1230000/BidPublicInfoService04/getBidPblancListInfoServc01

# -------------------------------------------
# Slack Webhook (알림 채널)
# -------------------------------------------
# 설정 방법:
# 1. Slack Workspace > Apps > Incoming Webhooks 검색
# 2. Add to Slack > 채널 선택 (#입찰-알림 권장)
# 3. Webhook URL 복사하여 아래에 입력
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL
SLACK_CHANNEL=#입찰-알림

# -------------------------------------------
# 크롤링 설정 (고급)
# -------------------------------------------
CRAWL_INTERVAL_HOURS=8
IMPORTANCE_THRESHOLD=2
