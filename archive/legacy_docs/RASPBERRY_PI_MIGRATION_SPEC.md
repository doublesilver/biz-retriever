# 라즈베리파이 통합 서버 구축 프로젝트 사양서

> **프로젝트명**: Cloud-to-Pi Migration  
> **목표**: Vercel/Railway 의존성 제거 및 라즈베리파이 4 기반 통합 서버 구축  
> **작성일**: 2026-01-26

---

## 📋 프로젝트 개요

### 배경
- **현재 상황**: 
  - Frontend: Vercel 호스팅
  - Backend/DB: Railway 호스팅
  - 월 비용 발생 및 플랫폼 의존성 존재
  
- **목표**:
  - 라즈베리파이 4 하나로 모든 서비스 통합 운영
  - Docker Compose 기반 컨테이너화
  - Tailscale을 통한 외부 접속 환경 활용
  - 비용 절감 및 완전한 제어권 확보

### 기대 효과
- ✅ **비용 절감**: 클라우드 서비스 비용 → 전기세만
- ✅ **학습 효과**: DevOps, Docker, 네트워킹 실무 경험
- ✅ **포트폴리오**: 자체 서버 운영 경험 어필
- ✅ **완전한 제어**: 모든 데이터와 서비스 직접 관리

---

## 🖥️ 하드웨어 사양

### 라즈베리파이 4 스펙
```
- Model: Raspberry Pi 4 Model B
- RAM: 8GB
- CPU: BCM2711 (Cortex-A72) @ 2.0GHz (Overclocked)
- Architecture: ARM64 (aarch64)
- Storage: microSD (Log2Ram 설정 완료)
- Swap: 2GB
- OS: Raspberry Pi OS (64-bit) 또는 Ubuntu Server 22.04 LTS
```

### 네트워크 환경
- **Tailscale VPN**: 외부 접속 및 윈도우 노트북과 양방향 통신
- **도메인**: gagisiro.com (subway-board 서비스)
- **추가 도메인**: (사용자 인터뷰 필요)

---

## 🏗️ 시스템 아키텍처

### 전체 구조도
```
┌─────────────────────────────────────────────────────────────┐
│                    Raspberry Pi 4 (ARM64)                    │
│  ┌───────────────────────────────────────────────────────┐  │
│  │              Docker Compose Network                    │  │
│  │                                                         │  │
│  │  ┌──────────────────┐      ┌──────────────────┐      │  │
│  │  │  Nginx Proxy     │      │  PostgreSQL 15   │      │  │
│  │  │  Manager         │◄─────┤  (Shared DB)     │      │  │
│  │  │  (Port 80/443)   │      │  (Port 5432)     │      │  │
│  │  └────────┬─────────┘      └──────────────────┘      │  │
│  │           │                                            │  │
│  │  ┌────────▼─────────┐      ┌──────────────────┐      │  │
│  │  │  subway-board    │      │     Redis        │      │  │
│  │  │  (Node.js)       │◄─────┤  (Port 6379)     │      │  │
│  │  │  (Port 3000)     │      │                  │      │  │
│  │  └──────────────────┘      └──────────────────┘      │  │
│  │                                                         │  │
│  │  ┌──────────────────┐                                  │  │
│  │  │  biz-retriever   │                                  │  │
│  │  │  (FastAPI)       │                                  │  │
│  │  │  (Port 8000)     │                                  │  │
│  │  └──────────────────┘                                  │  │
│  │                                                         │  │
│  │  ┌──────────────────┐                                  │  │
│  │  │  Portainer       │  (Optional: 컨테이너 관리 UI)   │  │
│  │  │  (Port 9000)     │                                  │  │
│  │  └──────────────────┘                                  │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
                    Tailscale VPN
                            │
                            ▼
                   Internet (gagisiro.com)
```

### 컨테이너 구성

#### 1. Nginx Proxy Manager
- **역할**: 리버스 프록시, SSL 인증서 자동 관리
- **포트**: 80 (HTTP), 443 (HTTPS), 81 (Admin UI)
- **기능**:
  - gagisiro.com → subway-board:3000
  - (추가 도메인) → biz-retriever:8000
  - Let's Encrypt SSL 자동 갱신

#### 2. subway-board (Node.js)
- **기술 스택**: Node.js, Express, Socket.io
- **포트**: 3000 (내부)
- **DB 연결**: PostgreSQL (db:5432)
- **Redis 연결**: redis:6379

#### 3. biz-retriever (FastAPI)
- **기술 스택**: Python 3.11, FastAPI, Celery
- **포트**: 8000 (내부)
- **DB 연결**: PostgreSQL (db:5432)
- **Redis 연결**: redis:6379
- **추가 서비스**: Celery Worker, Celery Beat

#### 4. PostgreSQL 15
- **역할**: 공유 데이터베이스
- **포트**: 5432
- **데이터베이스**:
  - `subway_board_db`
  - `biz_retriever_db`
- **볼륨**: `/home/admin/projects/data/postgres`

#### 5. Redis
- **역할**: 캐싱, Celery 브로커
- **포트**: 6379
- **볼륨**: `/home/admin/projects/data/redis`

#### 6. Portainer (Optional)
- **역할**: Docker 컨테이너 관리 UI
- **포트**: 9000

---

## 📦 프로젝트 구조

```
/home/admin/projects/
├── docker-compose.yml           # 전체 서비스 통합 설정
├── .env                         # 공통 환경 변수
├── nginx-proxy-manager/
│   └── data/                    # NPM 설정 데이터
├── subway-board/
│   ├── Dockerfile
│   ├── .env
│   ├── package.json
│   └── src/
├── biz-retriever/
│   ├── Dockerfile
│   ├── .env
│   ├── requirements.txt
│   └── app/
├── data/
│   ├── postgres/                # PostgreSQL 데이터
│   ├── redis/                   # Redis 데이터
│   └── backups/                 # DB 백업 파일
└── scripts/
    ├── backup-db.sh             # DB 백업 스크립트
    ├── restore-db.sh            # DB 복구 스크립트
    └── monitor.sh               # 리소스 모니터링
```

---

## 🔧 기술 스택

### 인프라
- **컨테이너**: Docker, Docker Compose
- **리버스 프록시**: Nginx Proxy Manager
- **VPN**: Tailscale
- **OS**: Raspberry Pi OS 64-bit / Ubuntu Server 22.04

### 데이터베이스
- **PostgreSQL**: 15-alpine (ARM64 호환)
- **Redis**: 7-alpine (ARM64 호환)

### 애플리케이션
- **subway-board**: Node.js 18+
- **biz-retriever**: Python 3.11, FastAPI, Celery

### 모니터링 (Optional)
- **Portainer**: 컨테이너 관리
- **Prometheus + Grafana**: 메트릭 수집 및 시각화 (추후 추가)

---

## 🚀 마이그레이션 로드맵

### Phase 1: 준비 및 설계 (1일)
- [/] SPEC.md 작성
- [ ] 사용자 인터뷰 (필요 정보 수집)
- [ ] 아키텍처 확정
- [ ] 프로젝트 디렉토리 구조 생성

### Phase 2: Dockerizing (2일)
- [ ] subway-board Dockerfile 작성
- [ ] biz-retriever Dockerfile 검토/수정 (ARM64 호환)
- [ ] docker-compose.yml 작성
- [ ] 로컬 테스트 (윈도우 노트북)

### Phase 3: 데이터 마이그레이션 (1일)
- [ ] Railway PostgreSQL 백업 추출
- [ ] 백업 파일 라즈베리파이로 전송
- [ ] DB 복구 스크립트 실행
- [ ] 데이터 무결성 검증

### Phase 4: 라즈베리파이 배포 (1일)
- [ ] 라즈베리파이에 프로젝트 복사
- [ ] Docker Compose 실행
- [ ] Nginx Proxy Manager 설정
- [ ] SSL 인증서 발급

### Phase 5: 도메인 연결 및 테스트 (1일)
- [ ] gagisiro.com DNS 설정 변경
- [ ] Tailscale을 통한 외부 접속 테스트
- [ ] 서비스 정상 작동 확인
- [ ] 성능 테스트

### Phase 6: 모니터링 및 최적화 (1일)
- [ ] 리소스 사용량 모니터링
- [ ] 메모리/CPU 제한 조정
- [ ] 자동 재시작 설정 검증
- [ ] 백업 자동화 스크립트 설정

---

## ⚙️ 리소스 제한 설정

### 컨테이너별 리소스 할당 (총 8GB RAM 기준)

| 컨테이너 | Memory Limit | CPU Limit | 우선순위 |
|----------|--------------|-----------|----------|
| PostgreSQL | 2GB | 1.0 | 높음 |
| Redis | 512MB | 0.5 | 중간 |
| subway-board | 1GB | 1.0 | 높음 |
| biz-retriever | 1.5GB | 1.0 | 높음 |
| Celery Worker | 1GB | 0.5 | 중간 |
| Nginx Proxy Manager | 512MB | 0.5 | 중간 |
| Portainer | 256MB | 0.25 | 낮음 |
| **여유 메모리** | **~1.2GB** | - | 시스템 |

### 안정성 설정
```yaml
restart: always
healthcheck:
  interval: 30s
  timeout: 10s
  retries: 3
```

---

## 🔐 보안 고려사항

### 1. 네트워크 보안
- ✅ Tailscale VPN으로 외부 접속 제한
- ✅ Nginx Proxy Manager로 SSL/TLS 암호화
- ⚠️ PostgreSQL/Redis는 외부 포트 노출 금지 (내부 네트워크만)

### 2. 인증 및 권한
- 모든 서비스에 강력한 비밀번호 설정
- `.env` 파일 Git 제외 (.gitignore)
- 정기적인 비밀번호 변경

### 3. 백업
- 일일 자동 백업 (cron)
- 백업 파일 암호화
- 외부 저장소 동기화 (윈도우 노트북)

---

## 📝 필요한 정보 (사용자 인터뷰)

### 1. 도메인 및 네트워크
- [ ] gagisiro.com 외 추가 도메인이 있나요?
- [ ] biz-retriever 서비스용 도메인은 무엇인가요?
- [ ] Tailscale IP 주소는 무엇인가요?
- [ ] 라즈베리파이의 로컬 IP는 무엇인가요?

### 2. 데이터베이스
- [ ] Railway PostgreSQL 접속 정보 (호스트, 포트, 사용자명, 비밀번호)
- [ ] 현재 데이터베이스 이름 (subway-board, biz-retriever 각각)
- [ ] 데이터베이스 크기는 얼마나 되나요?

### 3. 환경 변수
- [ ] subway-board의 현재 `.env` 파일 내용
- [ ] biz-retriever의 현재 `.env` 파일 내용
- [ ] API 키 (G2B, Gemini, Slack Webhook 등)

### 4. 프로젝트 위치
- [ ] subway-board 프로젝트는 어디에 있나요? (GitHub 저장소 URL)
- [ ] biz-retriever는 현재 `c:\sideproject`에 있는 것이 맞나요?

### 5. 운영 정책
- [ ] 서비스 다운타임 허용 시간은? (마이그레이션 중)
- [ ] 백업 주기는 어떻게 설정할까요? (일일/주간)
- [ ] 모니터링 알림은 어디로 받으시겠습니까? (Slack/Email)

---

## 🎯 성공 기준

### 필수 (Must Have)
- ✅ 모든 서비스가 라즈베리파이에서 정상 작동
- ✅ gagisiro.com 도메인 정상 접속
- ✅ SSL 인증서 자동 갱신
- ✅ 데이터 무손실 마이그레이션
- ✅ 서비스 자동 재시작 (장애 시)

### 권장 (Should Have)
- 🔄 Portainer를 통한 컨테이너 관리
- 🔄 일일 자동 백업
- 🔄 리소스 사용량 모니터링

### 선택 (Nice to Have)
- 💡 Prometheus + Grafana 대시보드
- 💡 로그 중앙화 (ELK Stack)
- 💡 CI/CD 파이프라인 (GitHub Actions → 라즈베리파이)

---

## 📚 참고 자료

### Docker Compose 예시
```yaml
version: '3.8'

services:
  nginx-proxy-manager:
    image: jc21/nginx-proxy-manager:latest
    restart: always
    ports:
      - "80:80"
      - "443:443"
      - "81:81"
    volumes:
      - ./nginx-proxy-manager/data:/data
      - ./nginx-proxy-manager/letsencrypt:/etc/letsencrypt
    networks:
      - pi-network

  postgres:
    image: postgres:15-alpine
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - ./data/postgres:/var/lib/postgresql/data
    networks:
      - pi-network
    deploy:
      resources:
        limits:
          memory: 2G
          cpus: '1.0'

  redis:
    image: redis:7-alpine
    restart: always
    volumes:
      - ./data/redis:/data
    networks:
      - pi-network
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '0.5'

networks:
  pi-network:
    driver: bridge
```

---

**작성자**: AI Agent  
**최종 수정**: 2026-01-26  
**상태**: 초안 (사용자 인터뷰 대기)
